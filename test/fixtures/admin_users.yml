# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one: {}
# column: value
#
two: {}
#  column: value

administrator:
	email: admin@mwbi.net
  encypted_password: <%= Devise::Encryptor.digest(User, 'administrator_pw') %>
	admin: true

laura:
	email: naturallaura@gmail.com
  encypted_password: <%= Devise::Encryptor.digest(User, 'laura_pw') %>
	admin: true

mary:
	email: mary@gmail.com
  encypted_password: <%= Devise::Encryptor.digest(User, 'mary_pw') %>
	admin: true